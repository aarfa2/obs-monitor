# OBS 机群监控系统 — 项目说明

**用途：** 向管理层说明本项目解决什么问题、能做什么、怎么部署、风险与边界。  
**日期：** 2026-08-20  
**范围：** 局域网内多台 OBS 推流电脑的运行监控（只看、不改、不遥控）。

---

## 1. 一句话

直播/录播现场有十几台电脑在用 OBS 推流。本系统让值班人员在**一个网页**上看到每台是否在推、卡不卡、掉没掉，出问题自动报警，并能回看近一天日志。它不管 OBS 怎么配、也不通过网页去改推流。

---

## 2. 背景与要解决的需求

现场常见情况是：OBS 在各机本地推流到服务器，采集卡、摄像头、编码器、上行网络任何一环出问题，画面就会卡、花、停，甚至整机静默。以往要发现问题，往往需要：

- 人走到机房看 OBS 窗口，或远程桌面一台一台点；
- 观众先投诉，再倒查；
- 出事后再翻各机 OBS 日志，时间对不齐、也容易漏。

核心需求可以归纳为四条：

1. **看得见：** 十几台机器的推流状态集中在一张网上，不用逐台登录。
2. **来得及：** 停推、重连、采集器掉线要能报警（通用 Webhook，可接企业微信/钉钉等），不必一直盯着屏。
3. **查得清：** 出问题后能区分是画面合成跟不上、编码器过载，还是上行网络不够，并检索近一天日志。
4. **控得住风险：** 只监控。不能通过这套系统去改 OBS、切场景、停推流，避免误操作和生产事故。

---

## 3. 项目意义（业务价值）

| 价值 | 说明 |
| --- | --- |
| 降低故障发现时间 | 从「观众反馈 / 巡机」变成「看板变红 + 即时通知」 |
| 降低排障成本 | 先看掉帧类型和日志分类，少盲目重启、少远程桌面 |
| 可规模复制 | 按「一台电脑一个采集器」扩展，10 台和 20 台同一套中心 |
| 保护生产安全 | 监控面与控制面分离，OBS 配置权仍在现场机 |

不做这套系统的代价，主要是：**故障发现依赖人，机器一多就看不过来；出了问题缺少统一的现场证据。**

---

## 4. 已具备的功能

### 机群总览

- 每台机器一张卡片：在线 / 离线、是否推流、是否重连、压力（正常 / 渲染 / 编码 / 网络）、视频采集源数量、码率、OBS CPU。
- 异常机器自动排到前面。
- 点进某一台，进入该机详情。

### 单机健康度

- 推流中、重连中、录像中、当前场景与源列表（视频 / 音频 / 其它）。
- 码率、帧率、拥塞。
- **三类掉帧（比单一 GPU% 更有用）：**
  - 渲染掉帧：合成跟不上（场景过重、浏览器源、滤镜等）；
  - 编码掉帧：编码器过载；
  - 网络丢帧：上行不够或服务器侧不稳。
- OBS 官方接口不提供 GPU 占用，系统不编造该数字；渲染掉帧即现场用来判断「显卡/合成压力」的指标。

### 报警（页面关掉也仍然有效）

由各机采集器判断 OBS 侧问题，由中心判断整机/采集器失联，统一走 Webhook：

- OBS 进程失联（WebSocket 断开且非正常退出）；
- 曾经推流后意外停止；
- 推流持续重连；
- 采集器超过心跳窗口未上报（电脑死机、进程被杀也能发现）。

报警带机器显示名；恢复后会再发一条恢复通知。支持「测试 Webhook」。

### 日志

- 读取各机 OBS 自己的日志（不是另造一套），推到中心查询。
- 网页可按分类（连接 / 编码 / 渲染 / 音频 / 源 / 系统 / 报警等）、级别、关键字检索。
- **只保留约 24 小时**，中心有条数上限；各机 OBS 超过一天的旧日志文件（正在写入的除外）由采集器清理。OBS 默认不会自己删旧日志。
- 推流密钥等敏感信息会打码。

---

## 5. 架构（给非研发也能看懂）

```
OBS 电脑 × N          局域网监控服务器              值班电脑/手机（局域网）
┌─────────────┐      ┌──────────────────┐      ┌─────────────┐
│ OBS 推流     │      │ 中心服务（Hub）    │      │ 浏览器看板   │
│ 采集器(无界面)│ ──出站──► │ 网页 + 报警 + 日志 │ ◄── │ 机群 → 单机 │
│ 只连本机 OBS │      │                  │      └─────────────┘
└─────────────┘      └──────────────────┘
```

- **每台 OBS 电脑：** 跑一个很轻的采集器（Agent）。它只连接本机 OBS，把状态推到中心。不在这台电脑上开网页，不装 Electron 一类带浏览器内核的壳，避免和推流抢内存/显卡。
- **监控服务器一台：** 跑中心服务，对外提供唯一看板地址，接收所有采集器，保存近一天日志，发送 Webhook。
- **值班人员：** 只访问服务器上的网页，不直接访问任何一台 OBS。

这样设计的原因：OBS 的远程接口本身具备改配置、停推流的能力。若把各机该接口直接暴露到局域网，等于把「遥控器」也放开。采集器只在本机只读采集、向外上报，中心**不会**向采集器下发任何控制命令。

多路视频采集卡属于「一台 OBS 里的多个源」，不需要一块卡一个程序；源列表会列出全部视频/音频源。

---

## 6. 技术评估（结论摘要）

| 项 | 评估 |
| --- | --- |
| 可行性 | 高。OBS 28+ 自带官方 WebSocket，指标与推流状态足够支撑监控。 |
| 与现有推流关系 | 采集器只读、约每秒一次轻量查询，正常情况下对编码影响可忽略。 |
| 资源占用 | 采集器为无界面进程（约数十 MB 内存量级），不适合用 Electron 打包。 |
| 规模 | 十余台、每秒一次快照，局域网完全可承受。 |
| 安装 | 中心一份服务；各机一份采集器 + 简短配置（服务器地址、密钥、显示名、本机 OBS 密码）。后续可打成安装包/开机服务，便于铺十几台。 |
| 明确做不到的 | 看不到观众端 CDN 卡顿；不能当「每路摄像机的单独监看画面」；不能替代推流服务器自身的状态。黑屏若编码仍在走，优先靠源状态和日志，而不是码率一个数。 |

技术选型上，采集与中心均为现有 Web 常用技术栈，不引入重桌面框架；看板为网页，局域网即可访问。

---

## 7. 安全与运维要点（建议写进制度）

1. 各机 OBS WebSocket **只监听本机**，不要映射到公网，也不要在防火墙上对全局域网开放该端口。
2. 采集器与中心用同一把接入密钥（Token）；机器有独立编号，禁止把一台的身份文件拷到另一台。
3. 本系统不能、也不应被当成 OBS 远程控制台使用。
4. 日志默认只留一天，避免十几台机器日志把磁盘堆满；需要更长保留期再单独立项。

---

## 8. 当前状态与建议下一步

**当前：** 机群看板、单机详情、三类掉帧、源列表、24 小时日志检索、Webhook 报警、采集器/中心分离，已在局域网场景可实际使用。

**建议下一步（按投入由小到大）：**

1. 采集器做成开机服务 + 简易安装包（仍无界面），方便铺到十余台。
2. 把 Webhook 接到现有值班群，约定谁响应「失联 / 停推」。
3. 若业务需要「出门也能看」，再做采集器出站到固定中心（仍不要把 OBS 端口打到公网）。

---

## 9. 给决策的一句话

这是一套**面向生产推流机房的值班监控**，不是 OBS 美化插件，也不是导播台。它用很小的本机占用，把「十几台机器好不好」变成可看、可查、可叫人的状态，同时把误操作风险关在门外。
